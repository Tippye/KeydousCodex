# Windows NJ98 验收记录 · 2026-09-06

## 0.4.0 Windows 系统托盘常驻（2026-09-08）

- 窗口右上角关闭会取消销毁并隐藏窗口，BridgeApp、Hook 状态读取、旋钮热键和已启用的 RGB 联动继续由原进程持有。托盘默认动作／“打开”恢复同一窗口；托盘“退出”与应用内退出仍进入原有完整清理路径。
- 六项桌面生命周期测试覆盖关闭隐藏、托盘恢复、托盘退出、页面退出、初始化失败和托盘事件循环意外结束；任一托盘故障都会停止应用，不留下不可达的隐藏硬件所有者。
- 全套 101 项 Python 测试中 100 项通过、1 项可选真实 Codex 测试跳过；Node 界面并发配置回归通过。
- 首次冻结验收准确暴露托盘图标资源名不一致，EXE 按设计在显示窗口前退出。资源查找统一到打包器实际保留的 `icon_256x256.png` 后重新构建；`Verify-DesktopPackage.py` 实际验证窗口关闭后进程和健康接口继续、窗口不可见、重复启动恢复窗口、应用内明确退出码为 0。
- `Verify-Package.py` 对最终冻结产物通过真实隔离 Hook 安装、信任、执行、禁用、移除和正常退出。发布 ZIP 的 SHA-256、CRC、运行文件、托盘依赖许可证和运行数据排除检查通过。
- 本轮没有写入键盘映射、屏幕或 RGB，没有修改真实用户 Hook 信任与 `windows/data`。托盘图标的实际 Windows 通知区创建由冻结应用成功启动作为证据；托盘菜单动作由同一生产回调的单元生命周期测试覆盖。

## 0.3.2 旋钮按下显示／隐藏（2026-09-07）

- 用户已确认 0.3.1 三个动作实机测试通过，并要求 Codex 已唤起时再次按下隐藏。TaskStartSnapshot：`5aa099d`，工作区干净；运行时原三个动作计数为左转 6、右转 22、按下 2。
- 按下以真实前台窗口状态决定行为：当前前台属于 Codex 则最小化到任务栏，否则恢复/聚焦。保留任务栏入口和现有窗口枚举，不缓存显示状态；左右旋转只聚焦并导航。复用现有三个键盘组合键，不重写设备。
- Python 99 项：98 通过、1 项可选真实生命周期跳过；Node 回归通过。新增覆盖前台最小化、后台聚焦、最小化恢复、多窗口目标选择、旋转不隐藏、失败及退出时不报告成功。
- `Verify-KnobHost.py --toggle` 在独立真实 Windows 窗口验证最小化、恢复到前台及恢复后导航，退出码 0；证据 `data/knob-host-acceptance/toggle-result.json`。未自动操作 Codex 界面，新切换手感仍待用户确认。
- 0.3.2 EXE 已构建；`Verify-DesktopPackage.py` 窗口、单实例和退出通过。使用现有 data 目录启动新版后，health 为 0.3.2，旋钮服务 running=true、无错误，可见窗口正常响应。没有再次写入键盘映射。
- 复杂度控制：仅扩展现有窗口操作函数，没有新增状态文件、硬件协议或常驻组件。

## 0.3.1 Windows 旋钮验收（2026-09-06）

- TaskStartSnapshot：`06db6ae`，工作区干净。用户要求优先适配旋钮并测试。改动范围为普通层三个旋钮动作、应用内 Windows 热键、设置入口和测试；未修改真实 Hook 信任、屏幕资源或灯光设置。
- `Verify-KnobDevice.py` 已在 NJ98 USB 上通过写入、完整读回、恢复测试，退出码 0。设备 key `ed87f4d711cc30cc`；只修改普通层槽 109/110/102，从音量减/音量加/静音改为 Ctrl+F9/F10/F11。finally 后两层 512 字节均与测试前一致。证据保存在 `data/knob-device-acceptance/result.json`。
- `Verify-KnobHost.py` 使用真实 RegisterHotKey、Windows 消息和 SendInput，在独立 Tk 测试窗口接收三次热键，收到两次导航组合；退出后可重新注册全部热键。退出码 0，证据保存在 `data/knob-host-acceptance/result.json`。测试仅替换目标窗口查找，不操作 Codex 界面。
- 生产窗口查找函数在交互桌面只读识别出当前 OpenAI.Codex 安装包窗口，返回句柄与 Windows 工具枚举一致。该结果不等同于 Codex 任务导航验收。
- Python 94 项测试：93 通过、1 项可选真实生命周期跳过。覆盖部分写入丢响应、恢复仅影响旋钮、保留 Fn 与其他改键、重复启用、外部冲突、热键冲突释放和焦点/修饰键阻止输入。Node 界面回归通过，包括新入口与失败后停止编辑。
- 0.3.1 EXE 构建成功；`Verify-DesktopPackage.py` 原生窗口、单实例、关闭流程通过，退出码 0。
- `Verify-Package.py` 冻结 EXE 的隔离 Hook 生命周期与退出通过，退出码 0。2026-09-07 新版桌面应用已用现有 `windows/data` 启动，真实 API 启用旋钮后 `knob_configured=true`、`pending=false`，三个槽位再次读回匹配。正常退出并重启后热键服务自动恢复；可见设置页展示运行状态和恢复入口。启动时使用可见窗口模式；隐藏启动会隐藏原生界面，不作为日常桌面启动方式。
- 2026-09-07 用户明确反馈“测试通过”，确认 0.3.1 实际左转、右转、按下的导航及窗口聚焦效果。热键计数本身仅表示软件路径成功；此次端到端验收依据用户的实机反馈，不从计数推断。
- 复杂度控制：设备写入仍由 MappingService/IoTClient 单一所有者管理，单键与旋钮复用同一事务写入函数和恢复日志；新增 knob.py 只负责 Windows 输入生命周期。未引入通用脚本执行器、键盘记录器或独立常驻服务。

## 0.3.0 Windows 桌面窗口验收

- Windows 默认启动独立 WebView2 窗口，复用现有界面和用户配置；无自动浏览器回退。
- 84 项 Python 测试：83 通过、1 项可选真实 Codex 测试跳过；Node 界面回归通过。
- 新增四项桌面生命周期测试覆盖窗口关闭、页面退出、引擎失败、空白页超时；平台测试覆盖桌面目录错误后的完整清理。独立有界代码复核通过。
- `Verify-Desktop.py` 在真实 Windows 用户会话通过 WebView2 渲染、160×80 预览、设置布局无横向溢出、PNG 导出及页面退出。测试代码仅在验收窗口接受退出确认，不改变生产界面。
- `Verify-DesktopPackage.py` 对 0.3.0 发布 EXE 验证可见原生窗口、健康响应、重复启动返回并激活原窗口、WM_CLOSE 后正常退出。
- `Verify-Package.py` 对同一发布 EXE 在隔离 CODEX_HOME 中通过真实 Hook 安装、信任、执行、禁用、移除和正常退出。
- 上述桌面测试未写键盘，也未修改真实用户 Hook 信任。键盘协议与 macOS 原生输入代码本轮不变；下述硬件和 Mac 验收限制继续适用。

本次实现位于 Keyphore 的 windows/，基于上游提交 9ca11f275809ebc2649bf365effdee8cc7682084。

## 已验证

- Windows 11 x64；Python 3.12.6、Pillow 12.3.0、PyInstaller 6.22.2。
- 首版 54 项 Python 测试中 53 项通过，1 项真实 Codex 可选测试单独在隔离 CODEX_HOME 中通过；0.2.0 新鲜验证结果见下方。
- JavaScript 保存期间继续编辑的回归测试通过，语法检查通过。
- 独立规格与代码质量审查完成：Hook 状态核心、安装生命周期、宠物、应用/HTTP/事件/RGB、界面。发现的问题已修正并复核。
- IoT 127.0.0.1:3814 实机识别 NJ98，id=1021、VID=0x3151、PID=0x4015，有线在线；电量未返回，保持未知。
- 按 NJ98 最终命令 0x07/0x87 写入工作颜色并读回一致；七个原始字段全部恢复、读回一致。详见 protocol-research.md 的纠错与读回记录。
- 实际 Windows 用户上下文通过界面导入九个本机 Codex 宠物；沙盒 Appx 列表为空的问题未通过硬编码安装路径掩盖。
- 浏览器检查 Codex 工作动画、160×80 宠物/状态栏布局和审阅窗口；授权复选框默认未选，启用按钮禁用。
- 实际打包 EXE 验收 Verify-Package.py：启动与健康响应、160×80 PNG、冻结后的独立 Hook EXE、八条真实 Codex Hook 安装/信任/复核、两秒内无输出地记录测试事件、主界面观察到 waiting、禁用/移除、退出码 0。全程隔离 CODEX_HOME，无设备写入。
- 跨进程互斥与退出期间取消、停止新操作已测试；RGB 恢复记录不会在失联或读回不一致时清除。
- 用户于 2026-09-06 确认：work 状态动画已上传到实际屏幕并正确显示。这是用户目视验收反馈，本轮助手没有再次操作设备；具体槽号、固件版本、帧数与耗时未记录。

## 尚未验证或实现

- work 动画以外的状态、三个槽位逐一覆盖与断电后保留行为尚未验收。协议数据包、列优先 RGB565、分片、取消和轮询恢复已有测试，传输 ACK 不代替用户目视验收。
- 默认固件黑猫区域替换、原生动态状态层共存、实时画面刷新或页切换，没有可用证据；当前版本仅手动上传 GIF。
- 上传的状态栏是当时快照。Windows NumLock/Caps 状态不是键盘独立遥测；无线通道号及电量没有数据时显示未知。
- 其他 NJ98 变体和其他型号未实机验收；精确允许列表之外不开放硬件写入。
- 用户已自行安装并信任真实配置中的插件；应用缓存的上次验证时间为 2026-09-06 13:22:42。本轮隔离测试没有修改该信任，真实用户新任务的全部事件链仍未逐项验收。
- RGB 写入是否全部为易失性存储未知，因此默认关闭、每次运行重新开启、至少十秒间隔，短暂状态可能被合并。不要与官方网页的写入或音乐/屏幕跟随同时运行。

## 复验

### 0.2.0 本轮新增证据（2026-09-06）

- 最终公共代码测试：`python -m unittest discover -s tests -v` 共 79 项，78 项通过、1 项真实 Codex 可选测试跳过。随后重新构建 Windows EXE，`Verify-Package.py` 单独通过 `PACKAGED_EXE_HOOK_LIFECYCLE_PASS` 与 `PACKAGED_SHUTDOWN_PASS`。两次验证均退出码 0。
- Keyphore 风格首页和分组设置已完成；独立规格、质量审查通过。Node 回归覆盖异步草稿保存、真实改键渲染、后端断开后状态失效与草稿保留。实际打包页面读取 101 个 NJ98 控件，页面无横向溢出。
- NJ98 普通层/Fn 层都读到 512 字节。Fn 槽65、Esc 槽0、A 槽9和计算器槽90与源码吻合。分别临时修改普通层和 Fn 层的计算器控件，写后读回成功，恢复后两层完整字节与初始矩阵一致。初始/最终 revision `0312083d90043f559da8aadbaec708e0b25e5079eac9279f54f9b876aa39073f`。没有测试物理按键触发，不能声称所有媒体/组合动作已实测。
- 映射模块独立规格/质量审查通过；模拟丢失响应、取消、部分恢复、备份损坏、外部变化和型号替换均有校验。真实验收脚本 `Verify-Mapping.py` 默认只读；`--write --bank normal|fn` 显式临时写入计算器控件并在 finally 恢复。
- Windows 0.2.0 打包 EXE 已通过隔离的真实 Codex 插件安装、Hook 执行、状态观察、禁用/移除和正常退出测试。未通过这项测试修改用户真实 Hook 信任。
- Python 平台分支与 Mac Fn 控制接口使用模拟测试；Mac 原生组件仍缺真实 Mac 构建与授权、输入输出验收。Windows 上通过的测试不得描述为 macOS 已通过。

从 windows/ 使用已准备的 Python 环境执行：

```powershell
python -m unittest discover -s tests -v
node tests/test_web_state.js
python Verify-Package.py
```

Verify-Package.py 需要先 Build.ps1，且没有另一份桥接程序运行。RGB 实机复验是显式操作：`python Verify-NJ98.py --rgb --data-dir data`，会临时写入工作颜色并在 finally 中恢复原设置。测试失败时应保留恢复记录并检查原因。

## Mac 源码验证范围

- `macos/build/report_state_tests.exe` 和 `keyboard_teardown_state_tests.exe` 均在 Windows/MSVC 编译并由根代理运行，退出码 0。覆盖多页按键状态、引用计数、释放和“失去 readiness 不等于设备销毁”的生命周期回归；不覆盖实际 DriverKit 通信。
- 四个 shell 构建/装配脚本通过 Bash 语法检查；Windows EXE 的构建成功不能作为 Mac 构建证据。
- 原生输入目前仅针对 ANSI NJ98 USB。物理 Caps Lock 指示灯同步尚未实现；不兼容的共享 VirtualHIDDevice 驱动会明确报错，程序不自动替换或移除其他应用共享的驱动。
- 尚需真实 Mac 上构建、签名、公证、系统权限、官方 IoT 连通性、普通/媒体键透传、Fn/Globe、睡眠唤醒、断线和退出释放验收。Mac 完整功能交付状态为待验证，整体任务不能按双平台全部通过关闭。

证据评级：Windows 已验证范围 B；双平台完整产品 C。公共状态、屏幕/RGB/固件协议仍各由一个模块管理；新增 Mac 输入组件只承担原生输入生命周期，原版 Swift/NuPhy 代码继续保留为上游产品和对照来源。

原生组件已通过独立的有界规格复核和源码质量审查；此前释放确认、异常后恢复、通信总超时及 Release 测试失效问题已修复并复核。该审查不能代替 Mac 编译或实机验收。
